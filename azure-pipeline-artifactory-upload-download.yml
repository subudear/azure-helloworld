trigger:
- master

variables:
  - group: artifactory

jobs:
- job: BuildAndTest
  workspace:
    clean: all
  pool:
    name: Self-Hosted-Agent

  #container: avcosystems/golang-node:1.16
  steps:
  - task: GoTool@0
    inputs:
      version: '1.17'
  - task: Go@0
    displayName: "Go Get"
    inputs:
      command: 'get'
      arguments: '-d'
      workingDirectory: '$(System.DefaultWorkingDirectory)'
  - task: Go@0
    displayName: "Go Build"
    inputs:
      command: 'build'
      workingDirectory: '$(System.DefaultWorkingDirectory)'
  - task: Go@0
    displayName: "Go Test"
    inputs:
      command: 'test'
      arguments: './...'
  - task: CopyFiles@2
    displayName: "Copy files to ArtifactStaging"
    inputs:
      TargetFolder: '$(Build.ArtifactStagingDirectory)'
  - task: PublishBuildArtifacts@1
    displayName: "Publish Artifact"
    inputs:
       artifactName: drop


  - task: ArtifactoryGenericUpload@2
    inputs:
      artifactoryService: 'BasicAuth-artifactory'
      specSource: 'taskConfiguration'
      fileSpec: |
        {
          "files": [
            {
              "pattern": "deployment.yaml",
              "target": "uploadordownload-generic-local/"
            }
          ]
        }
      failNoOp: true
      
  - task: ArtifactoryGenericDownload@3
    inputs:
      connection: 'BasicAuth-artifactory'
      specSource: 'taskConfiguration'
      fileSpec: |
        {
          "files": [
            {
              "pattern": "uploadordownload-generic-local/deploymeny.yaml",
              "target": "$(Build.ArtifactStagingDirectory)"
            }
          ]
        }
      failNoOp: true